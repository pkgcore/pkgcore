#!/usr/bin/env pkgcore-ebuild-helper

OPTIONS=(
	"--cwd=\"${PWD}\""
	"--dest=\"${DESTTREE}\""
	"--insoptions=-m0755"
)

# python side handles install
__ebd_ipc_cmd ${HELPER_NAME} "${OPTIONS[*]}" "$@"
